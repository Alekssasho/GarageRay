#![allow(non_snake_case)]

// List all external crate
// extern crate cgmath;

// All root level modules must be added here
mod accelerators;
mod cameras;
mod core;
mod integrators;
mod lights;
mod math;
mod ray;
mod samplers;
mod shapes;
mod spectrum;

fn main() {
    // Parse command lines
    // Init function

    // Cleanup function
}
